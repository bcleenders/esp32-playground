#include "Constants.h"

/**
 * This file should be ignored by git (see .gitignore).
 * Making changes to it (i.e. filling in my actual wifi password)
 * should be 'safe'. That is, the real values will not be checked
 * into git.
 */
const char *Constants::ssid = "foo";
const char *Constants::password = "foo";
const char *Constants::url = "foo";
